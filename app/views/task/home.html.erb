<h1>The Queen of Task Lists</h1>

<h2> Directions for Usage</h2>
<ul>
  <li>Click on the person's name you wish to see more details for.</li>
  <li>Press the delete button to delete a person's task.</li>
  <li>Click the Create a Task link to make a new item on the list!</li>
</ul>

<table>
<thead>Honey-Do List</thead>
<tr>
  <th>ID</th>
  <th>Completed</th>
  <th>name</th>
  <th> Mark as Complete </th>
  <th> Delete </th>
</tr>
<tr>
<% @tasks.each do |task|  %>

    <td> <%= task.id %> </td>
      <!-- linking the task person name to the unique task id (read)-->
<td><%= task[:completed] %></td>
<td> <%= link_to "#{task[:name]}", "/tasks/#{task.id}/show" %></td>

 <!-- Completed -->
<td> <%= button_to "Complete", "/tasks/#{task.id}/complete", method: "patch", data: {:confirm => "Mark #{task.description} by #{task.name} as complete?"} %> </td>

<!-- Delete -->
<td> <%= button_to "Delete", "/tasks/delete/#{task.id}", method: "delete",
      data: {:confirm => "Are you sure you want to delete?"} %>
</td>
</tr>
      <% end %>
</table>
