<div class ="index">


  <h1> Task List </h1>
<p> Click on an individual task name to view its description </p>

<table>
  <tr>
    <th>Name</th>
    <th>Date Completed</th>
  </tr>

  <% @tasks.each do |task| %>

  <tr>
    <td>
      <%= link_to "#{task.name}",
      "tasks/#{task.id}" %>
    </td>
    <td>
      <%= task.completed_at %>
    </td>
    <td>
      <%= link_to "Delete", "tasks/confirm/#{task.id}" %>
    </td>
  </tr>

  <% end %>
</table>


<p class="index_delete">
<%= link_to "Create New Task", "tasks/new" %>

</p>
</div>
