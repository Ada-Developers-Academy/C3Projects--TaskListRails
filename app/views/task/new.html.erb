<h1> So much to do! So little time!</h1>
<%= form_for @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :completed_at %>
  <%= f.date_field :completed_at %>

  <%= f.label :assign_to %>
  <%= f.collection_select :person_id, Person.all, :id, :name %>

  <!-- <%= f.select :person_id, options_for_select(%w(Bob Spot Tyrone)) %> -->
  <!-- <% @person.to_a.each do |person| %> -->
    <!-- <%= f.select :person_id, options_for_select(%w(person.name)) %> -->
  <!-- <% end %> -->

  <%= f.submit 'Add Task' %>
<% end %>

<%= render partial: "footer_links" %>
