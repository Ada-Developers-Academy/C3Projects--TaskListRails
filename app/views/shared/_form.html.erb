<%= form_for @task, url: @form_url, method: @method do |f| %>
<fieldset>
  <legend>Make a New Task</legend>
  <%= f.label :name %>
  <%= f.text_area :name %>

  <%= f.label :description, placeholder: "optional"%>
  <%= f.text_area :description %>

  <%= f.label :completed, 'Completed'  %>
  <%= f.check_box :completed %>
  
  <%= f.hidden_field :date_completed, value: Time.now %>

  <%= f.collection_select(:person_id, Person.all, :id, :name) %>
  <%= f.submit %>
</fieldset>
<% end %>
