<%= form_for(@task) do |f| %>
  <div>
    <%= f.label :name, "Task:" %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :description, "Description:" %>
    <%= f.text_field :description %>
  </div>

  <div>
  <%= collection_select( :task, :person_id, @all_people, :id, :name, { prompt: '< Assign Task >' } ) %>
    <%# alternative option: #%>
    <%# collection_select( :task, :person_id, @all_people, :id, :name, { :selected => 3 } ) %>
  </div>

  <%= f.submit %>
<% end %>
