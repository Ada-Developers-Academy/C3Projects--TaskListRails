<h2>Add Game</h2>

<%= form_for @task do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description, {:placeholder => "optional"} %>
  </fieldset>
  <%= f.submit %>
<% end %>
