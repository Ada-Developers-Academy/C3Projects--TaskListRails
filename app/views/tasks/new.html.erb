<h1>Add New Task</h1>

<%= form_for @task do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>
  </fieldset>
  <%= f.submit %>
<% end %>
