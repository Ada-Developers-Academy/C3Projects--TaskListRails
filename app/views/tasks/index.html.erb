<table>
  <thead>
  	<tr>
  	  <th>Task</th>
  	  <th class="checkmark">Completed?</th>
  	</tr>
  </thead>
  <tbody>
    <% @all_tasks.each do |task| %>
      <tr>
      	<td class="task_name_link"><%= link_to task.name, "tasks/#{task.id}" %></td>
        <% if task.completed? %>
        	<td class="checkmark"><%= task.completed? %></td>
        <% else %>
          <td><%= link_to "Mark Complete", "tasks/#{task.id}/completed", method: :patch %></td>
        <% end %>
        <td class="delete_link"><%= link_to "delete", "tasks/#{task.id}/delete" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Add a new task", "tasks/new", class: "default_button" %>
