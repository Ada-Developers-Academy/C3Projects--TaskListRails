
<%= form_for @task do |f|%>
  <fieldset>
    <legend>Add a new task</legend>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_field :description %>
  </fieldset>
  <%= f.submit 'Add new task' %>
<% end %>
